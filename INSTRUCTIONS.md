# カラクリ 公式サイト 制作指示書（Cursor 用）

> このドキュメントは Cursor に読み込ませて使う前提の制作指示書です。
> いきなり全部作らせず、「12. 制作フェーズ」に沿ってフェーズごとに区切って進めてください。
> 各フェーズ完了時に一度止めて、こちらで確認してから次へ進みます。
>
> **仮実装で進めた箇所の後追い作業は [PENDING.md](./PENDING.md) に必ず追記する**（12.1 参照）。
> **クライアント確認フェーズ（Phase 6）では [CLIENT_REVIEW.md](./CLIENT_REVIEW.md) のチェックリストを使う。**

---

## 1. プロジェクト概要

- 対象: 学生団体「カラクリ」の公式サイト
- 構成: 1 ページ LP（トップ）＋ 下層ページ（News 一覧 / News 詳細 / Member 一覧 / 個人ページ）
- 性格: 学生団体のブランディングサイト。3 原色（赤・青・黄）とオーガニックなブロブ装飾を使ったポップで遊びのあるデザイン。
- 元デザイン: Adobe XD（別途共有）。**装飾シェイプ・ロゴ・アイコン類は XD から書き出した素材を使う**こと。Cursor 側で形を再現生成しない。

### 1.1 参考サイト（[マルイス](https://maruisu.net/)）の扱い

クライアントから「マルイスを大パクリ」と言われているが、**コピーしてよいのは UX・構成の参考に限る**。文言・写真・ロゴ・配色の流用はしない。

| 参考にする（マルイス） | カラクリ固有（XD・指示書） |
|---|---|
| 1 枚 LP ＋ お知らせ一覧への導線 | 赤・青・黄ブロブ、ロゴ、各セクションの波形 |
| セクション縦積み（ヒーロー → お知らせ → メンバー → …） | About / SNS セクション、マルイスにない装飾 |
| お知らせカード（サムネ＋タイトル＋「続きを読む」系） | microCMS 連携、詳細ページ |
| **ハンバーガー押下後のフルスクリーンオーバーレイ**（MENU / CLOSE、**2×2 グリッド**） | メニュー項目・象限イラスト・リンク先はカラクリ用（XD 準拠） |
| フッターのシンプルなリンク群 | 団体名・コピーライトはカラクリ用文案 |

マルイスは静的でヒーローにスクロール演出はない。**カラクリ独自の目玉は XD のヒーローアニメ（5.1.1）**。

---

## 2. 技術構成・制約

- **HTML5 / CSS3 / Vanilla JavaScript のみ**
- フレームワーク・ビルドツール・パッケージマネージャは使わない（npm install 不要、ビルドステップなし）
- そのままブラウザで開けば動く構成にする
- News のみ microCMS から **クライアントサイド fetch** で取得（ビルド時生成はしない）
- 問い合わせは Google フォーム（**別タブで開くボタン遷移**。iframe 埋め込みは使わない）
- ホスティングは静的ホスティング想定（後で決定。どこでも動くよう外部依存を増やさない）

CSS は素の CSS で書く（Sass などのプリプロセッサも使わない）。CSS 変数（custom properties）はカラー・余白の管理に積極的に使ってよい。

---

## 3. ディレクトリ構成

```
/
├── index.html              … トップ（Top/About/News/Member/SNS/Contact を1枚に）
├── news/
│   ├── index.html          … News 一覧（microCMS から取得）
│   └── detail.html         … News 詳細（URL クエリ ?id= で1ファイル運用可）
├── member/
│   ├── index.html          … Member 一覧
│   └── honda.html          … 個人ページ（Message）※メンバーごとに1ファイル
├── css/
│   └── style.css
├── js/
│   ├── main.js             … ハンバーガーメニュー・スムーススクロール共通
│   ├── hero.js             … トップヒーローのスクロール連動アニメ（5.1.1）
│   └── news.js             … microCMS 取得・描画（一覧・詳細）
├── images/                 … XD から書き出した装飾・ロゴ・アイコン・写真
│   └── hero/               … ヒーロー用ブロブ（個別 SVG/PNG。1枚合成は避ける）
└── README.md               … 引き渡し用の更新手順メモ（13 章参照）
```

ページ間で共通のヘッダー/フッター/メニューは、各 HTML に直書きで構わない（インクルード機構は持たない）。ただしマークアップは全ページで完全に揃える。

---

## 4. デザインシステム

> 正確な値は XD のインスペクタから拾って差し替えること。以下は読み取りベースの初期値。

### カラー（CSS 変数で定義）
```css
:root {
  --color-red:    #C8202A;  /* XDの実値に合わせる */
  --color-blue:   #1B3A8C;  /* 〃 */
  --color-yellow: #E8A800;  /* 〃 */
  --color-ink:    #1A1A1A;  /* 本文テキスト */
  --color-bg:     #FFFFFF;
}
```

### フォント
- 本文（日本語）: Noto Sans JP（Google Fonts）
- 英語見出し（News / Member / SNS / Contact / Message / Profile 等）: XD の指定フォントを確認して合わせる。指定がなければ上品なセリフ系で。
- ウェイトは見出し太め・本文標準。

### 装飾
- 背景のブロブ／波形シェイプは **XD から SVG または PNG で書き出し**て `images/` に配置し、`background-image` または `<img>` で配置する。CSS で似た形を自作しない。
- ロゴ（2×2 のカラフルな四角）も XD から書き出した画像を使用。
- **ヒーローのうねうねはブロブごとに個別ファイル**で書き出す（アニメで個別に動かすため。1枚にまとめた背景画像だけでは不可）。

### 共通パーツ
- ボタン: 角丸の塗り or 枠線ボタン（XD に合わせる）。ホバーで軽く色/不透明度変化。
- セクションごとに背景色が変わる（News=青 / Member=黄 / SNS=赤 など）。テキスト色は背景に対して可読なものを選ぶ。

---

## 5. ページ仕様

### 5.1 トップ `index.html`

上から順に縦に並べる 1 ページ構成（[マルイス](https://maruisu.net/) と同様の LP 流れ）。各セクションに `id` を振り、メニューからアンカーリンクで飛べるようにする。

1. **Hero / Top** — ロゴ＋ブロブ装飾。**スクロール連動アニメあり（5.1.1）**。
2. **About**「学生団体カラクリ」— 見出し＋紹介テキスト（波形の白背景）。ヒーローアニメ完了後、自然にこのセクションが見える配置。
3. **News**（青背景）— microCMS から最新 **3 件**を取得しカード表示。「一覧を見る」→ `news/`。カードは詳細へリンク（マルイスの「続きを読む」に相当）。
4. **Member**（黄背景）— 見出し＋紹介。「メンバー紹介」→ `member/`（マルイスの「メンバー紹介」ボタンと同役割）。
5. **SNS**（赤背景）— Instagram / TikTok / Facebook アイコン（`target="_blank" rel="noopener"`）。**URL は後差し替え**（13 章）。
6. **Contact**（白背景）— Google フォームへ飛ぶボタン（7 章）。

固定ヘッダーにハンバーガーメニュー（5.6）。

#### 5.1.1 ヒーロー・スクロールアニメ（クライアント要望）

XD の **Web 1920 - 1（ページ先頭）→ Web 1920 - 2（少しスクロール後）** の遷移を実装する。

**意図（クライアント言葉の整理）**

- 最初: うねうね（ブロブ）が**中央付近に集まり**、ロゴが主役。
- スクロールに応じて: 赤・黄・青のブロブが**それぞれ別の動き**で画面端へ広がる（「ジャングルを抜ける」＝**パララックスで道が開く**イメージ）。
- 結果: 中央に白いエリアが広がり、**About のテキストが読める状態**になる（別ページへの切り替えではない）。

**実装方針**

- ブロブは HTML 上で**個別要素**（`<img>` またはインライン SVG）。2 状態の PNG 切り替えだけでは不十分。
- ヒーロー領域はスクロール進行用に高さを確保（例: `min-height: 150vh` 〜 `200vh`）し、`position: sticky` またはスクロール進捗 `0〜1` で各ブロブの `transform`（`translate` / `scale`）を制御。
- ブロブごとに移動量・速度係数を変え、手前ほど大きく動かす。
- ロジックは `js/hero.js`。外部ライブラリ（GSAP 等）は使わない（2 章）。
- **SP ファースト**で係数・終了位置を決め、PC（`min-width: 768px` 以降）で XD の Web 1920 値に合わせて調整。

**先方確認用（実装前に一言あるとよい）**

> 指をスクロールしている間、ブロブが指の動きに追従して開いていく想定で合っていますか？

#### 5.1.2 マルイスとの対応（トップ）

| マルイス | カラクリ |
|---|---|
| キャッチ＋リード文のヒーロー | ロゴ＋ブロブアニメ → About 文 |
| お知らせ 6 件＋一覧 | News 3 件＋`news/` |
| メンバー紹介＋ボタン | Member＋`member/` |
| アクセス | SNS ＋ Contact（フォームボタン） |

---

### 5.2 News 一覧 `news/index.html`

- microCMS から記事一覧を取得し、カードのグリッドで表示（XD / マルイス参考: カード複数列、SP は 1 列）。
- 各カード: サムネイル / タイトル / 日付。クリックで **詳細ページ**へ（`news/detail.html?id={contentId}` など）。

### 5.3 News 詳細 `news/detail.html`

- URL の `id`（または `slug`）で microCMS から **1 件取得**し、`title` / `publishedAt` / `thumbnail` / `body`（リッチエディタ HTML）を表示。
- 取得失敗・ID なし時はフォールバック文言。
- 一覧へ戻るリンクを置く。

### 5.4 Member 一覧 `member/index.html`

- メンバーを役割カテゴリ（代表 / 広報 など、XD のカテゴリ見出しに従う）ごとに分けて、顔写真＋名前＋肩書で一覧表示。
- 内容は **静的に HTML へ直書き**（microCMS 連携なし）。
- 個人ページを持つメンバーは「続きを読む」等で個人ページへリンク。

### 5.5 個人ページ（Message）`member/honda.html`

- XD の本田晴喜さんページ準拠: 顔写真 /  names・肩書 / メッセージ本文 / Profile ブロック。
- メンバーごとに 1 ファイル複製して作る（テンプレ HTML を 1 つ用意 → 複製して中身差し替え）。

### 5.6 ハンバーガーメニュー

- 全ページ共通。右上の **MENU / CLOSE** で**フルスクリーンのオーバーレイ**を開閉（[マルイス](https://maruisu.net/) の MENU / CLOSE の操作感を参考）。
- **レイアウト: 画面全体を 2×2 グリッド**（破線の十字で区切り）。各セルにイラスト＋ラベル＋リンク。
- **象限とリンク（確定配置）**

  | 位置 | ラベル | 遷移先 |
  |------|--------|--------|
  | 左上 | News | `news/`（トップは `news/index.html`） |
  | 右上 | Member | `member/` |
  | 左下 | About | トップ `#about` |
  | 右下 | Contact | トップ `#contact` |

- **象限イラスト（適用済み）**: `images/icons/newsmenu.png` / `membermenu.png` / `aboutmenu.png` / `contactmenu.png`
- **周辺の色ブロブ装飾**: 現状 CSS 仮（**PND-009**）。XD 書き出しで差し替え。
- 開閉は `main.js`（`aria-expanded`、フォーカス、Escape、メニュー内リンク押下で閉じる）。MENU 閉じるときは右上の青い **CLOSE** バー。

---

## 6. microCMS 連携（News）

### スキーマ（API: `news`、リスト形式）

| フィールド ID | 種類 | 用途 |
|---|---|---|
| `title` | テキスト | 記事タイトル |
| `thumbnail` | 画像 | カードサムネイル |
| `body` | リッチエディタ | **詳細ページの本文** |
| `publishedAt` | （システム標準） | 日付表示に利用 |

### 取得方針

- クライアントサイドで `fetch` する。SDK は使わず素の fetch でよい。

```js
// js/news.js のイメージ
const SERVICE_DOMAIN = "XXXX";
const API_KEY = "XXXXXXXX";

async function fetchNewsList(limit = 10) {
  const res = await fetch(
    `https://${SERVICE_DOMAIN}.microcms.io/api/v1/news?limit=${limit}`,
    { headers: { "X-MICROCMS-API-KEY": API_KEY } }
  );
  const data = await res.json();
  return data.contents;
}

async function fetchNewsDetail(id) {
  const res = await fetch(
    `https://${SERVICE_DOMAIN}.microcms.io/api/v1/news/${id}`,
    { headers: { "X-MICROCMS-API-KEY": API_KEY } }
  );
  return res.json();
}
```

- トップは `limit=3`、一覧は `limit=10`（必要に応じ調整）。
- 詳細は `fetchNewsDetail(id)` で 1 件。
- 取得失敗時のフォールバック（「お知らせを読み込めませんでした」等）を必ず入れる。
- 日付は `YYYY.MM.DD` などに整形して表示。
- 詳細の `body` は microCMS から返る HTML をそのまま表示する領域を用意（サニタイズ方針は信頼できる管理画面前提。必要なら `README` に注意書き）。

### 注意

- クライアントサイド fetch なので **API キーがソースに露出する**。microCMS 側で **読み取り専用（GET のみ）API キー**を発行し、可能なら参照元ドメイン制限をかけること。

---

## 7. 問い合わせ（Google フォーム）

- Contact セクションは **「お問い合わせはこちら」等のボタン**で Google フォームを **別タブ**で開く（確定。iframe 埋め込みは使わない）。
- フォームは **引き渡し用 Gmail アカウント**で作成し、回答先（スプレッドシート含む）がそのアカウントに残るようにする（13 章）。
- フォーム URL は後で差し替え。`href` にプレースホルダ＋ HTML コメントを残す。

---

## 8. レスポンシブ

- **SP ファースト**で実装し、`min-width` のメディアクエリでタブレット・PC に拡張（確定）。
- ブレークポイント目安: `768px`（タブレット）/ `1024px`（PC）。XD の SP アートボードを優先し、Web 1920 は PC 調整用。
- 画像・装飾は `max-width: 100%` 基本。ヒーローブロブは SP で画面外にはみ出さないようクランプ。
- ハンバーガーは SP・PC ともフルスクリーンオーバーレイ（マルイス同様）。
- タップ領域は SP で 44px 目安。

---

## 9. JavaScript 要件

- `main.js` … ハンバーガー、スムーススクロール、メニュー閉じる、`prefers-reduced-motion` 時はヒーローアニメを簡略化する連携も可。
- `hero.js` … ヒーローのスクロール進捗計算・ブロブ個別 `transform`（5.1.1）。
- `news.js` … 一覧・トップ 3 件・詳細 1 件の取得と描画、エラーフォールバック。
- 外部ライブラリは入れない。

---

## 10. コーディング規約

- セマンティックな HTML（`header` / `nav` / `main` / `section` / `footer`、見出しレベルを正しく）。
- 画像に `alt`、装飾画像は `alt=""`。
- クラス名は分かりやすく（BEM 風でなくてよいが一貫性を持たせる）。
- インライン style は使わず CSS に集約。
- メタタグ（title / description / OGP / favicon）を全ページに設定。
- 日本語サイトなので `<html lang="ja">`。
- [マルイス](https://maruisu.net/) の HTML/CSS をそのままコピーしない（構成・UX の参考のみ）。

---

## 11. 確定事項（先方合意済み）

| 項目 | 内容 |
|---|---|
| レスポンシブ | **SP ファースト** |
| News 詳細 | **あり**（microCMS の `body` を使用） |
| Contact | **ボタンで Google フォームを別タブ開き** |
| SNS URL | **後から差し替え**（仮は `#` またはコメント付きプレースホルダ） |
| 参考サイト | [マルイス](https://maruisu.net/) … **ページ構成・カード導線・ハンバーガーオーバーレイ**のみ。見た目・文案は XD / カラクリ |
| ヒーロー | **スクロール連動でブロブが開くアニメ**（5.1.1） |

---

## 12. 制作フェーズ（このフェーズ順で進める／各フェーズ後に停止）

- **Phase 0**: ディレクトリ構成と空ファイル一式、CSS 変数・リセット・フォント。`images/hero/` 用プレースホルダ。
- **Phase 1**: トップの**静的レイアウト**（Hero はアニメ終了位置＝Web 1920-2 寄りでも可 / About / Member / SNS / Contact）。News はダミーカード 3 枚。XD 未着の装飾は **CSS 仮**で進め、[PENDING.md](./PENDING.md) に後追い手順を書く。
- **Phase 1b**: **ヒーロースクロールアニメ**（`hero.js`）。Web 1920-1 → 2 の開き。`prefers-reduced-motion` では終了状態を初期表示。
- **Phase 2**: ハンバーガー（フルスクリーン **2×2** オーバーレイ）＋スムーススクロール（`main.js`）、全ページ共通ヘッダー/フッター骨組み。
- **Phase 3**: microCMS（`news.js`）。トップ 3 件、一覧、**詳細ページ**。
- **Phase 4**: Member 一覧＋個人ページテンプレート。
- **Phase 5**: レスポンシブ最終調整（SP → PC）、メタタグ/OGP、`README.md`。
- **Phase 6**（**現在**）: **クライアント確認・XD 合わせ・本番素材投入**。[PENDING.md](./PENDING.md) の `open` を順に `done` 化。[CLIENT_REVIEW.md](./CLIENT_REVIEW.md) のチェックリストで確認。
- **Phase 7**: **公開・引き渡し**（13 章）。ドメイン・DNS・ホスティング・Gmail 連携アカウント・README 運用手順。

各フェーズで、「ブラウザでどう開いて何を確認すればよいか」を一言添えること。

**「サイト完成」の定義**

| 段階 | 条件 |
|------|------|
| プロトタイプ完成 | Phase 0〜5 完了（**現在地**） |
| コンテンツ・素材完成 | [PENDING.md](./PENDING.md) の必須 `open` がすべて `done` |
| 公開完了 | Phase 7 完了（本番 URL で全ページ・News・フォームが動作） |

### 12.1 仮実装と PENDING.md

- XD 素材や URL が未着のときは **仮（プレースホルダ）でレイアウトだけ進めてよい**。
- その都度 **[PENDING.md](./PENDING.md) に ID（`PND-xxx`）・差し替え手順・確認方法を追記**する。
- コード側は `data-placeholder="PND-xxx"` を付け、完了時に削除する。
- 一覧表（サマリー）と詳細手順の **両方**を更新すること。

---

## 13. 公開・引き渡しフロー（運用メモ）

引き渡しは **クライアント名義の Gmail を親アカウント**に集約する。

- Gmail に紐づけるもの: Google フォーム、microCMS、ホスティング/ドメイン、（任意）Analytics / Search Console
- `README.md`: microCMS で News を追加・編集する手順、ログイン情報の在り処
- ドメイン取得 → DNS → 公開確認 → アカウント移管

---

## 14. 未確定・クライアント確認したいこと

Phase 6 で確認・確定する。対応する PENDING ID と [CLIENT_REVIEW.md](./CLIENT_REVIEW.md) のセクションを併記。

| # | 確認項目 | PENDING / 参照 |
|---|----------|----------------|
| 1 | **フォント指定**（XD の英語見出しフォント名） | CLIENT_REVIEW §デザイン |
| 2 | **カラー実値**（XD インスペクタ） | CLIENT_REVIEW §デザイン → `css/style.css` の変数 |
| 3 | **SNS リンク先 URL** | PND-008 |
| 4 | **Google フォーム URL** | PND-006 |
| 5 | **microCMS** サービス ID / 読み取り専用 API キー / 試し記事 | PND-010 |
| 6 | **ヒーローブロブ・ロゴ**の XD 書き出し | PND-001, PND-002 |
| 7 | **セクション波形**の XD 書き出し | PND-003 |
| 8 | **メニュー周辺ブロブ**（象限イラストは適用済み） | PND-009（ブロブのみ） |
| 9 | **文案**（About 等） | PND-007 |
| 10 | **メンバー写真・名簿・個人ページ** | PND-011, PND-012 |
| 11 | **本番ドメイン・OGP・favicon** | PND-013 |
| 12 | **ホスティング先** | 13 章 / CLIENT_REVIEW §公開 |
| 13 | **ヒーローアニメの体感**（スクロールで開くイメージ） | CLIENT_REVIEW §トップ |
| 14 | **メンバー更新頻度** … 頻繁なら将来 microCMS 化を検討（現状は静的） | 方針確認のみ |

---

## 15. クライアント確認チェックリスト

詳細な確認順・チェックボックスは **[CLIENT_REVIEW.md](./CLIENT_REVIEW.md)** を使う。

- 打ち合わせ前: ローカルで `index.html` を開き、MENU・スクロール・各ページ遷移を一通り見る。
- 打ち合わせ中: CLIENT_REVIEW のセクション順に画面共有し、OK / 要修正をメモ。
- 打ち合わせ後: 確定事項を PENDING の該当 ID に反映し、素材・URL を受け取ったら差し替え → `done`。
